# .env.example
# ────────────────────────────────────────────────────────────────
#   Copy this file to .env and fill in your real values
#   NEVER commit .env or any token files to git!
# ────────────────────────────────────────────────────────────────

# Required ── get these from https://dev.kick.com/apps
KICK_CLIENT_ID=
KICK_CLIENT_SECRET=

# Optional ── change only if you use different values
KICK_REDIRECT_URI=http://localhost:8080/callback

# Where to store the OAuth tokens (relative to your script)
KICK_TOKEN_FILE=kick_tokens.json

# Local port for the temporary Flask server (OAuth callback)
KICK_FLASK_PORT=8080

# Default scopes to request (space separated)
KICK_SCOPES=user:read channel:read channel:write chat:write